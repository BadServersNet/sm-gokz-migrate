// Maps that were renamed on the Global API since the legacy database was created.
// gokz-migrate migrates the old map's times under the new name instead of deleting them.
// The dry run log lists candidates for every map that is no longer global.
//
// "Renames"
// {
//     "kz_oldname"    "kz_newname_v2"
// }
"Renames"
{
}
